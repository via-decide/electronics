# ESP32 lvgl Learning Example

This is a buildable, runnable example demonstrating the performance parameters of the `lvgl` subsystem.

## Commands
```sh
idf.py set-target esp32
idf.py build
idf.py flash monitor
```
