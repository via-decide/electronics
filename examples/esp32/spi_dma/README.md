# ESP32 spi_dma Learning Example

This is a buildable, runnable example demonstrating the performance parameters of the `spi_dma` subsystem.

## Commands
```sh
idf.py set-target esp32
idf.py build
idf.py flash monitor
```
